export const metadata = { title: "The interface", description: "The tree, splits and tabs, scrollback and settings." }

# The interface

The TUI shows a tree of machines, projects, branches, agents and terminals beside a main area with tabs and splits. This page covers each part and the settings that shape it.

## The sidebar

```text
MACHINES
▾ ● local                        ⠋1 ⚑1
  ▾ ▤ Workspace                      2
    ▾ ◆ api                      ⠋1 ⚑1
      ▾ Branches                     7
          ● main                    +1        checked out in the repo, uncommitted lines
          ◇ conch/add-health-check  ⠋ ↑3      own worktree, agent working, 3 ahead of main
          · feat/login          #12✓  ↑2      not checked out, pull request with passing checks
          … 18 more
      ▾ Agents                       2
          ⠋ Add health check   conch/add-health-check
          ! Fix flaky tests    fix/flaky
      ▾ Terminals                    1
          › dev-server         main
    ▸ ◆ web
  ▾ ❯ CLI                          1
    ▾ Terminals                    1
        › zsh
```

Each machine has two groups. `Workspace` holds its projects (folders you added with `a`); selecting it shows every project with what runs in it, and the tab bar lists the tabs of all of them. `CLI` holds a machine's agents and terminals that belong to no project. `n` or `c` on the machine (or on `CLI`) starts one there, in your home folder.

### Projects

Projects appear when you open a pane inside a git repository, or add them with `a`, which opens a folder browser on the selected machine (local or remote):

| Key | In the folder browser |
| --- | --- |
| `enter` | open a folder |
| `←` | go up |
| `a` | add the selected folder |
| `.` | add the folder you're in |
| `/` | filter |
| `g` | jump to a typed path |
| `n` | create a folder |
| `p` | create a new project (a folder with a git repository and an initial commit) |

Git repositories show `◆ git`, existing projects `◆ project`. From the CLI: `conch project add PATH` or `conch project create [-no-git] PATH`. Linked worktrees belong to their main repository. The list is kept in `~/.config/conch/projects.json`.

### Branches

Branches list checked-out branches, the base branch, branches with panes and recent ones; the rest fold into `… N more`. Each shows uncommitted changes (`+42 −7`, `⚠` conflicts), commits ahead/behind the base (`↑2 ↓1`), and the state of agents working on it. Git state refreshes when git metadata changes, after an agent uses a tool, and every few seconds.

### Pull requests

Pull requests come from the `gh` CLI (your existing GitHub login): `#12✓` checks passing, `#13✗` failing, `#14●` running, purple when merged. `o` opens the branch's PR. They refresh every minute while agents work in the project, every five minutes otherwise, and on `R`.

### Agents

Agents are named after their task (Claude's title) until you rename them with `r`. Their states are described in [Agents](/docs/agents#agent-states); every project also has a [Sessions](/docs/sessions) section.

### Fold state

Fold state and sidebar width are remembered in `~/.config/conch/ui.json`.

### Changes view

Selecting a branch shows its **changes**: its pull request (checks, review), uncommitted files (or files changed since the base when it isn't checked out), commits ahead, and diffs.

| Key | In the changes view |
| --- | --- |
| `↑↓` | select a file |
| `enter` | show its diff |
| `o` | open the PR |
| `y` | copy the file path or the whole diff |
| `esc` | back |

## Splits and tabs

The main area has tabs (click them, `+` adds one, `×` closes), and each tab can be split into several views: panes, branch changes, projects or machines side by side. The focused split has the highlighted border.

Tabs work like tmux windows. Every agent and terminal you open from the tree gets its own tab, and opening it again jumps to that tab; a pane is never shown in two places. Projects, branches and sessions share a browsing tab that follows the tree as you move.

The tab bar lists the tabs of what you select in the tree: a project shows its tabs, `Workspace` those of all the machine's projects, `CLI` the machine's own, and `Agents` or `Terminals` (or an agent or terminal in them) narrows to those. A branch — or `Branches` — lists the tabs showing changes, not the project's agents and terminals. Each branch opens in its own tab and keeps it, so looking at another branch, or at the project's page, leaves it alone. `Branches` itself shows a page of every branch — including the ones the tree hides behind "… more" — with its worktree, uncommitted lines, pull request, how far it is from the base, and when it last changed. A tab belongs to the group of its first split. Moving onto a project, its branches or Sessions shows that page — in the group's browsing tab if there is one, otherwise as a preview — while the bar keeps listing the group's tabs. The machine row has no tabs and shows its page as a preview too. A preview becomes a tab once you open, split or type into it. `ctrl+b n`, `p` and the numbers move through the tabs listed; `ctrl+b w` lists every tab, grouped. Closing a split or a tab ends the agents and terminals in it, after you confirm, and a pane that exits (a shell's `exit`, an agent's `/exit`) closes along with its split or tab. Only a pane that fails within its first 10 seconds (a missing command, a bad flag) stays, marked exited, so you can read the error; `x` removes it.

| Keys | |
| --- | --- |
| `ctrl+b %` or `v` / `ctrl+b "` or `-` | split right / down (beside a terminal or agent, the new split starts a shell in its folder; otherwise it waits for you to open something) |
| `ctrl+b ←→↑↓`, `o`, `;` | move focus between splits: by direction, to the next, to the last one focused |
| `ctrl+b {` / `}` | swap the split with the previous / next |
| `ctrl+b q` | show split numbers; press a number while they show to focus that split |
| `ctrl+b space` | next layout: even-horizontal, even-vertical, main-horizontal, main-vertical, tiled (`ctrl+b alt+1`–`alt+5` pick one) |
| `ctrl+b ctrl+arrow` / `alt+arrow` | resize by 1 / 5 cells; press the arrow again within half a second to keep resizing |
| `ctrl+b x` | close the split; an agent or terminal in it ends, after you confirm |
| `ctrl+b c`, `n`, `p`, `0-9`, `l` | new, next, previous, go to, last tab (among the tabs listed for the tree selection) |
| `ctrl+b <` / `>`, `.` | move the tab left / right, or to a position you type (among the tabs listed) |
| `ctrl+b w` | pick any tab or split, grouped by project and CLI, with each split's branch or folder and agent state |
| `ctrl+b &`, `,` | close, rename tab |
| `ctrl+b =`, `z` | equalize splits, zoom |
| `ctrl+b S` | type into every agent and terminal in the tab at once, like tmux's `synchronize-panes`; their borders turn amber and the status bar shows `SYNC`. Press it again to stop. |
| `ctrl+b d` | detach (agents keep running) |
| `ctrl+b !`, `:` | jump to the next agent waiting for you, ask the brain |
| `v` / `s` / `O` in the tree | open the selected item in a split right / below / a new tab |

These follow tmux's default bindings; `ctrl+b 1` is the first tab and `0` the tenth. `ctrl+b !` jumps to the next agent waiting for you instead of breaking a pane out.

Drag a border between splits to resize. Tabs and splits are remembered in `ui.json`.

## Scrollback and copying

The wheel over a pane scrolls back through its history (10,000 lines); the view stays on the same text while output continues, and typing returns to the live screen.

`ctrl+b [` (or `ctrl+b pgup`) enters scroll mode with a cursor:

| Key | In scroll mode |
| --- | --- |
| `↑↓←→` / `hjkl` | move the cursor (scrolling at the edges) |
| `0` / `$` | line start / end |
| `pgup` / `pgdn` | page |
| `g` | oldest line |
| `v` or `space` | start a selection |
| `y` or `enter` | copy the selection |
| any other key | return to live |

Drag across a pane to select and copy; double-click copies a word. Copies use OSC 52 (works over SSH) plus `pbcopy`/`wl-copy`/`xclip` locally. `y` copies a branch name or directory in the tree, a file path or the whole diff in the changes view.

Programs that use the mouse themselves (vim, htop) still get the mouse; full-screen programs get arrow keys from the wheel.

## Settings

Press `,` or click `⚙` at the right end of the status bar. Changes are saved to `~/.config/conch/config.toml` (see [Configuration](/docs/configuration)).

### Theme

Colour schemes: Conch, Dracula, Catppuccin Mocha, Nord, Gruvbox Dark, Tokyo Night, One Dark, Solarized Dark, Rosé Pine, Kanagawa, Everforest Dark, Monokai, GitHub Dark, Ayu Dark and Night Owl (applied instantly).

Below them, an **Oh My Zsh prompt theme** for new zsh terminals, picked from the themes installed on this computer. conch starts zsh with a wrapper that loads your own `.zshenv`/`.zprofile`/`.zshrc`/`.zlogin` first and then switches the theme for that terminal only (`omz theme use`), so your dotfiles are never edited; "Keep my .zshrc theme" turns it off.

### Notifications

- A master switch.
- How: desktop notification, system sound, terminal beep.
- When: an agent is waiting, one finishes, and/or a plan limit is nearly used (Claude's or Codex's 5-hour or weekly window passing 80% and 95%, once per window).
- **Quiet hours** (22:00–08:00 and other presets, or any `quiet_start` / `quiet_end` in config.toml) and **snooze for an hour**. While silenced the status bar shows `🔕` (click it to resume); waiting agents still show in the sidebar.
- Send a test.

### Agents

Every machine with each supported agent, installed (with version) or not; `enter` on a missing one installs it there. The default agent chosen here is what `c` pre-selects and what tasks use.
